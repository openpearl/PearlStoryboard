module OpenpearlMove
  class PearlClass

    # clientContext is defined by the required data 
    # this module needs to operate.
    def initialize(clientContext)
      @clientContext = clientContext
    end

    def handleExerciseFrequency
      puts "What is up!"
      @clientContext.activityTimeToday < 30min
      


      return (which tags are valid)
    end

  end
end
